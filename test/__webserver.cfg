# Configuration File
# -------------------------------------------------------------

# 程序服务名称
SERVER_NAME=QC++ Web Server

# 服务运行的本机IP
SERVER_IP=192.168.146.128
# 服务监听端口号
SERVER_PORT=8088
# 服务监控端口号
MONITOR_PORT=7088

# 请求客户端Socket超时时间(毫秒)
SOCK_TIMEOUT=30000

# 通信线程数
COMM_THREAD_MAX=1
# 通信线程使用内存大小
COMM_BUFFER_SIZE=1024
# 通信线程监控超时时间(毫秒)
COMM_THREAD_TIMEOUT=10000

# 工作线程数
WORK_THREAD_MAX=18
# 工作线程使用内存大小
WORK_BUFFER_SIZE=1048576
# 工作线程监控超时时间(毫秒)
WORK_THREAD_TIMEOUT=20000

# 任务队列大小
TASK_QUEUE_SIZE=100

# HTTP协议请求头信息的最大内存
REQUEST_HEADER_SIZE=1024
# HTTP协议请求正文信息的最大内存
REQUEST_CONTENT_SIZE=1048576

# HTTP协议响应头信息的最大内存
RESPONSE_HEADER_SIZE=1024
# HTTP协议响应正文信息的最大内存
RESPONSE_CONTENT_SIZE=1048576

